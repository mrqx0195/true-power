# AGENTS.md

## 项目语言规范

- **思考过程**：使用简体中文。
- **日常交流**：使用简体中文。
- **代码注释**：使用简体中文。
- **Git Commit**：使用简体中文。

## 项目编写规则

- **简洁输出**：不要在对话中输出技术细节（应在代码中以中文注释写出），对话中应当只保留进度汇报、意见征求。
- **详细输出**：在代码中，所有**新增**内容应有完善、详细的简体中文注释、Javadoc。
- **默认Nonnull**：所有参数、字段、返回值均默认为**Nonnull**，若此处可能为`null`，应显式使用`@javax.annotation.Nullable`。
- **不逾矩**：未完全在要求中的改动，应当向用户征求意见后再进行修改。
- **其他规范**：
    - 声明时不得使用`var`
    - 实例在调用自身的字段及方法时，必须写出`this.`
    - 尽可能减少`private`的使用，改为使用`protect`或`public`
    - 尽可能减少`import static`的使用，只使用`import`

## 注释及Javadoc规范

- **规范使用**：类、类属性、类方法的注释必须使用 Javadoc 规范，不得使用`// xxx`。
- **简洁注释**：若一个方法的参数用途可从参数名推断（即“望文生义”，如`LivingEntity target`），则可省略对应的`@param`；
  若一个方法的返回值可从函数名推断（如`getXXX()`），则可省略对应的`@return`。
- **接口注释**：所有的抽象方法（包括接口中的方法）必须要用 Javadoc 注释、除了返回值、参数、
  异常说明外，还必须指出该方法做什么事情，实现什么功能。对子类的实现要求，或者调用注意事项，请一并说明。
- **内部注释**：方法内部单行注释，在被注释语句上方另起一行，使用`//`注释。方法内部多行注释使用`/* */`注释，注意与代码对齐，不得使用行尾注释。
- **注释要求**：第一、能够准确反应设计思想和代码逻辑；第二、能够描述业务含义，使别的程序员能够迅速了解到代码背后的信息。
  好的命名、代码结构是自解释的，注释力求精简准确、表达到位。避免出现注释的一个极端：过多过滥的注释，代码的逻辑一旦修改，修改注释是相当大的负担。
  反例：
  ```
    // put elephant into fridge
    put(elephant, fridge);
  ```
  方法名 put，加上两个有意义的变量名 elephant 和 fridge，已经说明了这是在干什么，语义清晰的代码不需要额外的注释。